export class PlatoMerienda {
    id?: string;
    platoMerienda: string;
    detalleMerienda: string;
    precioMerienda: number;
    userUID: string; // id del usuario logueado (el que crea el plato, es decir el ID del Restaurante)
}
